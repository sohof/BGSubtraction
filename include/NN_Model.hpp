#ifndef NN_Model
#define NN_Model
#include <opencv2/core.hpp>


// class NN_Model 
// {
// public:
//     // constructors
//     NN_Model() = default; //recommended to provide default if other cons. are defined.
//     explicit PatNN_Modelch(int id);
//     class NN_Model(cv::Mat, id, cv::InputArray data);
    
//     // const member functions
//     int getID() const ;
//     void displayPCAComponents() const;
//     std::vector<int> layer_sizes();

//     initialize_parameters()

// std::vector<int> m_VectorOfInts;
//     cv::Mat getPrincipalComponents();
//     cv::Mat backProject(const cv::Mat &vec) const ;
//     cv::Mat project(const cv::Mat &vec) const;

//     // member functions    
//     void calcPCA(cv::InputArray data); // not used at the moment

// private:
//     std::vector<int> layer_sizes();
//     long long int m_train = 0 ;
// };


#endif
